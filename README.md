# Igor Kovalevsky Resume Website

Editorial one-page resume website for a Sales Manager position.

## Commands

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run build
npm.cmd run preview
npm.cmd start
```

## Deploy

Build output is generated in `dist`. Railway runs the build command, then starts the production server with `npm.cmd start`.
